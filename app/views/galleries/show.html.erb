            <% @gallery.assets.each do |asset| %>
              <% if asset.assettype_id == 1 %> 

            
                <%= link_to asset.image.url(:medium) do %>
                  <%= image_tag asset.image.url(:thumb), :data => { :big => asset.image.url(:original), :title => asset.title, :description => asset.description }, :alt => "" %>
                <% end %>

              <% else %>
                <%= link_to asset.embeded_video, :data => { :title => asset.title, :description => asset.description } do %>
                  <span class="video">Watch this on Vimeo!</span>
                <% end %>
              <% end %>

            <% end %>

<section id="portfolio">
      <div id="portfolio-single" class="section-inner">

		<div id="custom-content" class="white-popup-block">

			<div class="row">
				<div class="col-sm-12 text-center">
			    	<div class="owl-carousel owl-theme portfolio-slider">
				    	<% @gallery.assets.each do |asset| %>
				      		<% if asset.assettype_id == 1 %> 
								<div class="item">
									<%= image_tag asset.image.url(:original) %>
								</div>
							<% else %>
								<div class="item-video">
									<%= link_to "", asset.embeded_video, :class => "owl-video" %>
								</div>
							<% end %>
		           		<% end %>		            		
		            </div>
		       </div><!-- .col -->
				
		
			</div><!-- .row -->

			<div class="popup-content">
				<div class="row">

					<div class="col-sm-12">

						<div class="popup-header">
							<h2 class="popup-title"><%= @gallery.name %></h2>
							<div class="popup-subtitle">
								<%= @gallery.sub %>
							</div>
							<div class="popup-line"></div>
						</div>

					</div>

				</div>

				<div class="row">

					<div class="col-sm-12">
						<%= @gallery.description.html_safe %>
					</div>

				</div>

			</div>

			<section class="popup-callout">
				<div class="row">

					<div class="col-sm-12">
						<h3>Like Our Works?</h3>
						<div class="module-line"></div>
					</div>

					<div class="col-sm-12">
						<div class="btn-list">
							<a href="index.html#services" class="btn btn-custom-3" target="_blank">View Services</a>
							<a href="index.html#contact" class="btn btn-custom-4" target="_blank">Start Project</a>
						</div>
					</div>

				</div>
			</section>

		</div>


    </div> <!-- END #portfolio-single .section-inner -->
</section> <!-- END SECTION #portfolio-->